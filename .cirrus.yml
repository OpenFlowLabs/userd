freebsd_test_task:
  name: cargo test FreeBSD (nightly)
  matrix:
    - freebsd_instance:
        image_family: freebsd-13-0-snap
  env:
    HOME: /tmp # cargo needs it
  install_script: curl https://sh.rustup.rs -sSf | sh -s -- -y --profile minimal --default-toolchain nightly; source $HOME/.cargo/env
  cargo_cache:
    folder: $HOME/.cargo/registry
  build_script: cargo build --all
  test_script: cargo test --all --all-targets
  before_cache_script: rm -rf $HOME/.cargo/registry/index